# WS-03 — Shared config package — Evidence

- Date: 2026-07-18
- Task card: `BACKLOG.md` → WS-03
- Status: done
- Commit: `266371e WS-03: add shared config package`

## DoD check

| DoD item | Result |
|---|---|
| A consumer package extends `tsconfig.base.json` and typechecks | ✅ throwaway `@hr/check-consumer` extended `@hr/config/tsconfig.base.json` via package exports — clean pass below |
| Shared ESLint config lints a sample file successfully | ✅ consumer imported `@hr/config/eslint` — clean pass below |
| Strict TypeScript on (`strict`, `noUncheckedIndexedAccess`) | ✅ proven by deliberate-failure runs below |

## Evidence

### Consumer runs (valid file `src/sample.ts`)

```
=== typecheck (valid file) ===
PASS: typecheck clean
=== lint (valid file) ===
PASS: lint clean
```

### Deliberate-failure runs (bad file `src/bad.ts` — unchecked index access + unused var)

```
=== typecheck ===
src/bad.ts(3,10): error TS2532: Object is possibly 'undefined'.
tsc exit code: 2

=== lint ===
src/bad.ts
  2:9  error  'unusedValue' is assigned a value but never used.
       Allowed unused vars must match /^_/u  @typescript-eslint/no-unused-vars
✖ 1 problem (1 error, 0 warnings)
eslint exit code: 1
```

### Resolved versions

typescript 5.9.3 · eslint 9.39.5 · prettier 3.9.5 · typescript-eslint ^8 (via `@hr/config`)

### Notes

- The consumer package was throwaway per the task card — removed before commit; `packages/contracts` (WS-04) becomes the first real consumer.
- `@hr/config` exposes `./eslint`, `./prettier`, and `./tsconfig.base.json` through package `exports`; the tsconfig `extends "@hr/config/tsconfig.base.json"` package-name form is confirmed working.
- Base lint config is deliberately minimal (recommended sets + unused-vars + consistent-type-imports); architecture-enforcing rules (boundaries, RTL) land in WS-08 where they are proven with deliberate failures.
