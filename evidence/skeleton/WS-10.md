# WS-10 — Local dev stack — Evidence

- Date: 2026-07-18
- Task card: `BACKLOG.md` → WS-10
- Status: done
- Commit: `4a05d32 WS-10: add local dev stack (postgres + redis)`

## DoD check

| DoD item | Result |
|---|---|
| `docker compose up -d` yields healthy Postgres and Redis | ✅ both `(healthy)` — output below |
| Connections verified | ✅ `psql select version()` and `redis-cli ping → PONG` below |
| Connection strings documented | ✅ `.env.example` (`.env` gitignored since WS-01) |
| Volumes persist across restarts | ✅ restart test below |

## Evidence

### Healthy services

```
NAME                   STATUS
hr-platform-postgres   Up 15 seconds (healthy)   0.0.0.0:5433->5432/tcp
hr-platform-redis      Up 15 seconds (healthy)   0.0.0.0:6380->6379/tcp
```

### Connection checks

```
PostgreSQL 16.14 (Debian 16.14-1.pgdg13+1) on aarch64-unknown-linux-gnu
redis-cli ping → PONG
```

### Persistence across `docker compose restart`

```
pg  row written before restart  → after restart: "survives restart"
redis key set before restart    → after restart: "survives restart"
(check artifacts cleaned up)
```

## Notes

- **Host ports are 5433 (Postgres) and 6380 (Redis)** — the defaults are occupied by another local project (`ksa-gro-platform`), which was left untouched. Documented in both `docker-compose.yml` comments and `.env.example`.
- Versions: Postgres 16.14, Redis 7 (AOF persistence on). Revisited against the chosen provider's managed offerings at WS-20 (ADR-006).
- Docker runs via colima on this machine; the daemon was already up.
