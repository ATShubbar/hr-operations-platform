# WS-02 — Monorepo scaffolding — Evidence

- Date: 2026-07-18
- Task card: `BACKLOG.md` → WS-02
- Status: done
- Commit: `1dc557e WS-02: scaffold pnpm + turborepo monorepo`

## DoD check

| DoD item | Result |
|---|---|
| `pnpm install` succeeds from a clean state | ✅ `node_modules` deleted, reinstalled clean — output below |
| `pnpm turbo run build` executes (no-op) across the empty workspace | ✅ 0 tasks, exit 0 — output below |
| Workspace globs match `apps/*`, `packages/*` | ✅ `pnpm-workspace.yaml`; placeholder `.gitkeep` in both |

## Evidence

### Clean install (`rm -rf node_modules && corepack pnpm install`)

```
devDependencies:
+ turbo 2.10.5

Done in 188ms using pnpm v10.34.5
```

### No-op turbo build (`corepack pnpm turbo run build`)

```
 WARNING  No tasks were executed as part of this run.

 Tasks:    0 successful, 0 total
Cached:    0 cached, 0 total
  Time:    6ms
```

### Pins

- Node: `22.14.0` (`.nvmrc`, `engines.node >= 22.14.0`) — matches installed `v22.14.0`
- pnpm: `10.34.5` pinned with integrity hash in `packageManager` (written by `corepack use pnpm@10`)
- turbo: `2.10.5` (devDependency `^2.5.0`)

### Environment note

`corepack enable` fails on this machine without sudo (`EACCES` on `/usr/local/bin`), so pnpm is invoked as `corepack pnpm …` here. Both paths are documented in the README getting-started section; CI (WS-09) will use corepack in the workflow so this is a local-ergonomics note only.
