# WS-04 — Shared contracts package — Evidence

- Date: 2026-07-18
- Task card: `BACKLOG.md` → WS-04
- Status: done
- Commit: `7b9596f WS-04: add shared contracts package with zod`

## DoD check

| DoD item | Result |
|---|---|
| Package builds (`tsc`) | ✅ `tsc -p tsconfig.build.json` clean (tests excluded from build output) |
| Example schema exports runtime validator + inferred TS type | ✅ `clientCompanySchema` + `type ClientCompany` (and `bilingualTextSchema`/`BilingualText`) from `src/client-company.ts` |
| Unit test: valid payload accepted, invalid rejected | ✅ 3/3 tests pass — output below |
| Lint + typecheck clean under shared config | ✅ both pass |

## Evidence

### Test run (Vitest)

```
 ✓ src/client-company.test.ts (3 tests) 2ms
 Test Files  1 passed (1)
      Tests  3 passed (3)
```

Cases: valid bilingual payload accepted; empty Arabic name rejected; malformed UUID rejected.

### Turbo pipeline run (`pnpm turbo run build test`)

```
 Tasks:    2 successful, 2 total
Cached:    0 cached, 2 total
  Time:    714ms
```

### Resolved versions

zod ^4 · vitest ^3 (workspace test runner standardized on Vitest per WS-04 approval)

### Notes

- The example schema uses a real architecture shape: client company with bilingual (`ar`/`en`) name fields per ADR-005.
- **Environment fix during this task:** turbo initially failed with "Unable to find package manager binary" because pnpm wasn't on PATH. Resolved without sudo via `corepack enable --install-directory ~/.local/bin` (already on PATH on this machine) — `pnpm` now works directly; the `corepack pnpm` fallback in the README remains valid for other machines.
