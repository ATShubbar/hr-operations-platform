# WS-13 — Production RLS pattern — Evidence

- Date: 2026-07-19
- Task card: `BACKLOG.md` → WS-13
- Status: done
- Commit: `WS-13: port validated RLS pattern into production code`

## DoD check

| DoD item | Result |
|---|---|
| Ported scenario tests pass against production code | ✅ 9/9 RLS scenarios (13/13 suite total) — output below |
| Migrations apply to a fresh DB | ✅ all 4 migrations + policies verified on `hr_platform_fresh`, then dropped |
| Policy template documented | ✅ client-scoped table checklist in `apps/api/src/modules/README.md` |
| Lint / typecheck / build green | ✅ all pass |
| Throwaway `ws11_check` removed | ✅ dropped by `replace_ws11_with_core_scope_check` migration; its e2e spec deleted |

## Evidence

### Test run

```
 Test Files  3 passed (3)
      Tests  13 passed (13)
```

RLS suite (`test/rls.e2e-spec.ts`, spike scenarios 1–8 ported): own-rows read, omitted-filter isolation, foreign-ID returns null, WITH CHECK blocks cross-client insert and update, unset scope = zero rows, staff cross-client read, 40-way interleave with zero bleed, rollback leaves no residue.

### Fresh-DB deploy with policies verified

```
All migrations have been successfully applied.
      polname      |   polroles
-------------------+--------------
 staff_full_access | {app_staff}
 client_isolation  | {app_client}
```

## Notes

- Production wiring: `PrismaService` = staff path (connects as `app_staff`); `ScopedPrismaService.forClient(clientId)` = client-rep path (connects as `app_client`, transaction-local scope per operation, `maxWait` 10s per spike finding 2). Migrations use the owner URL only.
- **Dev role passwords live in the migration** (idempotent `DO` block). Production MUST rotate via `ALTER ROLE … PASSWORD` at provisioning — flagged as a WS-20 checklist item.
- Automatic per-request path selection (auth resolves the caller → staff vs `forClient`) arrives with WS-14/WS-15 and Priority-2 auth; until then `core_scope_check` keeps the pattern continuously tested.
