# WS-16 — i18n scaffold (ar/en, runtime RTL switch) — Evidence

- Date: 2026-07-19
- Task card: `BACKLOG.md` → WS-16
- Status: done
- Commit: `WS-16: add i18n scaffold with runtime RTL switching`

## DoD check

| DoD item | Result |
|---|---|
| Same page correct in ar (RTL) and en (LTR), runtime-switchable | ✅ browser-verified: `/ar/rtl-check` and `/en/rtl-check` screenshots; clicking the switcher on `/ar` navigated to `/en` with translated content |
| Zero string literals in page components | ✅ grep found no literal JSX text; all copy flows through `t()` (counts below) |
| shadcn shortlist verified in RTL | ✅ checklist below — 8/8 pass |
| Production build green | ✅ `next build` with SSG for both locales |
| Lint (incl. RTL rule) green | ✅ `hr/rtl-safe-classes` passes over generated components |

## RTL component checklist (visually verified in browser, both directions)

| Component | RTL result |
|---|---|
| Button (primary, outline, secondary) | ✅ order and padding mirror correctly |
| Badge | ✅ |
| Card (header + content) | ✅ text right-aligned in ar |
| Input + Label | ✅ placeholder and caret right-aligned |
| Select | ✅ chevron mirrors to the left edge in RTL |
| Dialog | ✅ opens centered; title right-aligned; close (×) at the logical end (top-left in RTL) |
| Table (header + rows) | ✅ columns right-aligned, order mirrors |
| Language switcher link | ✅ sits at the logical end in both directions |

## String externalization proof

```
=== literal JSX text in app pages (expect none) ===
none found — all copy via t()
t() usage: page.tsx 3 · rtl-check/page.tsx 2 · layout.tsx 2 · language-switcher.tsx 2
```

## Notes

- **shadcn's CLI now has a first-class `--rtl` init flag** — generated components use logical properties throughout and pass our `hr/rtl-safe-classes` lint rule with zero fixes needed. Better outcome than the task card anticipated.
- **This shadcn generation is built on Base UI, not Radix**: composition uses `render={<Button/>}` instead of `asChild` (hit and fixed in the dialog trigger). Recorded so module developers don't trip on it.
- Default locale is `ar` per ADR-005; locale set + default live in `src/i18n/routing.ts` — adding a GCC locale = one entry + one catalog.
- `lang`/`dir` are set from the active locale in the `[locale]` layout, replacing WS-06's hardcoded values as planned.
