# WS-19 — Seed script — Evidence

- Date: 2026-07-19
- Task card: `BACKLOG.md` → WS-19
- Status: done
- Commit: `WS-19: add idempotent development seed`

## DoD check

| DoD item | Result |
|---|---|
| Seed runs clean against a fresh DB | ✅ run 1 below |
| Idempotent against an already-seeded DB | ✅ run 2 identical; row count stays 3 |
| Refuses `NODE_ENV=production` | ✅ exits 1 with explicit message |
| Harness + RLS tests still green after seeding | ✅ 24/24 |
| Lint / typecheck green | ✅ |

## Evidence

```
=== run 1 (fresh) ===
Seed complete: 3 seed rows across clients A (11111111-…) and B (22222222-…).
=== run 2 (idempotent repeat) ===
Seed complete: 3 seed rows across clients A (…) and B (…).
=== state check ===        3
=== production guard ===
Refusing to seed: NODE_ENV=production. The seed is development-only.
exit code: 1
```

## Notes

- **Scope honestly reduced per the approved card:** `clients`/`users` tables don't exist yet (Priority-2 modules), so the seed covers what exists — deterministic fixtures on the exemplar table — and establishes the framework: idempotency via a `seed:` ownership prefix (never touches test/manual data), environment guard, and exported well-known client IDs (`SEED_CLIENT_A/B`) as the contract shared with the isolation harness. Role/user seeding extends this file as those tables land (tracked in the Priority-2 epics).
- Runner: `tsx` (devDependency); script: `pnpm --filter @hr/api db:seed`.
