# WS-06 — Next.js web scaffold — Evidence

- Date: 2026-07-18
- Task card: `BACKLOG.md` → WS-06
- Status: done
- Commit: `dcfd37f WS-06: scaffold Next.js web app`

## DoD check

| DoD item | Result |
|---|---|
| Dev server renders the placeholder page | ✅ rendered and screenshot-verified via browser preview (page title, subtitle, contract card with Arabic text all visible) |
| Production build succeeds | ✅ `next build` output below — page statically prerendered |
| Tailwind works | ✅ styled card/spacing visible in render; logical utilities (`ps-6 pe-6`) present in served HTML |
| Page consumes `ClientCompany` from `@hr/contracts` | ✅ `clientCompanySchema.parse()` runs at build/render time (static prerender proves it executed without throwing); bilingual name rendered |
| Lint / typecheck clean | ✅ both pass |
| In turbo pipeline | ✅ `turbo run build --filter=@hr/web...` built contracts then web (2/2) |

## Evidence

### Production build (`pnpm turbo run build --filter=@hr/web...`)

```
@hr/web:build: ○  (Static)  prerendered as static content
 Tasks:    2 successful, 2 total
 Time:    9.549s
```

### Live render checks (dev server)

- Browser screenshot verified: heading "HR Operations Platform", card "Shared contract check (@hr/contracts)" showing `Example Company / شركة المثال` and `Status: active` — Arabic glyphs render correctly.
- Served HTML contains the contract data and logical utilities:

```
$ curl -s <dev-url> | grep -o 'Example Company...'
Example Company
$ curl -s <dev-url> | grep -c 'ps-6 pe-6'
1
```

### Notes

- Resolved versions: Next 15, React 19, Tailwind 4 (via `@tailwindcss/postcss`).
- Layout `lang`/`dir` are hardcoded `en`/`ltr` with a code note — they become dynamic in WS-16 (i18n scaffold).
- Deliberately no shadcn/ui or design work here; that arrives with RTL verification in WS-16.
- `.claude/launch.json` added with `web` and `api` dev-server configs (autoPort — port 3000 was occupied by an unrelated process on this machine).
- The root layout uses no remote fonts (system stack) so builds work offline and nothing is fetched from outside — consistent with the residency posture.
