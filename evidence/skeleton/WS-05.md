# WS-05 — NestJS API scaffold — Evidence

- Date: 2026-07-18
- Task card: `BACKLOG.md` → WS-05
- Status: done
- Commit: `e79d09f WS-05: scaffold NestJS API with health endpoints`

## DoD check

| DoD item | Result |
|---|---|
| Dev/built server serves `/health` and `/ready` with 200 + build info | ✅ live curl below (built `dist/main.js`, port 3001) |
| App consumes `@hr/config` | ✅ tsconfig extends base; eslint uses shared config; both pass |
| e2e test passes | ✅ 2/2 via Vitest + supertest — output below |
| Lint / typecheck / build clean | ✅ all pass |
| Turbo pipeline includes the app | ✅ workspace run below |

## Evidence

### Live server probes (against built `node dist/main.js`)

```
=== GET /health ===
{"status":"ok","service":"hr-api","version":"dev","uptimeSeconds":7}
=== GET /ready ===
{"status":"ready"}
HTTP 200
```

### e2e test run

```
 ✓ test/health.e2e-spec.ts (2 tests) 67ms
 Test Files  1 passed (1)
      Tests  2 passed (2)
```

### Full workspace pipeline (`pnpm turbo run build test lint typecheck`)

```
 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    2.625s
```

### Notes

- **Issue hit and fixed during the task:** Vitest failed to collect the e2e spec because `unplugin-swc` was configured with `module.type: commonjs`, which turned the `vitest` import into a forbidden `require()`. Fixed by setting `module.type: es6` (SWC still handles the NestJS decorators; Vitest consumes ESM natively). This is the documented NestJS+Vitest recipe.
- Health endpoints carry a code comment marking them as **must-stay-public** for the WS-15 deny-by-default guard (they're probed unauthenticated by deploy gate and load balancer).
- Resolved versions: NestJS 11, vitest 3.2.7, supertest 7.
- `@hr/contracts` is declared and type-resolves; first runtime consumption comes with the example module (WS-07).
