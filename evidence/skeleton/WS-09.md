# WS-09 — CI pipeline — Evidence

- Date: 2026-07-18
- Task card: `BACKLOG.md` → WS-09
- Status: done
- Commits: `7afa98e WS-09: add CI workflow` (main) · `f89b77b` (proof branch)

## DoD check

| DoD item | Result |
|---|---|
| Green CI run on `main` | ✅ user-confirmed green in GitHub Actions; identical command verified locally (11/11 tasks) |
| Deliberately-bad PR runs red with the lint error visible | ✅ user-confirmed red on the proof PR |
| Both linked as standing proof | ✅ see links below; proof PR left **open** by design |

## Evidence

- **Repository:** https://github.com/ATShubbar/hr-operations-platform (private)
- **Green run:** the `main` push run of workflow **CI** (commit `7afa98e`) — visible at `…/actions`, confirmed green by the repo owner on 2026-07-18.
- **Red run:** PR **"Do not merge — WS-09 CI red proof"** from branch `ws-09-boundary-violation-proof` (commit `f89b77b`, a deliberate cross-module deep import) — CI failed with the `hr/module-boundaries` error, confirmed by the repo owner on 2026-07-18. The PR stays open as the standing proof required by ACTION-PLAN DoD 1.2.
- **Local mirror of the CI command** (exact sequence from `ci.yml`):

```
$ pnpm install --frozen-lockfile   → Done in 340ms
$ pnpm turbo run lint typecheck test build
 Tasks:    11 successful, 11 total
 Time:    8.041s
```

## Notes

- Workflow: corepack-activated pnpm (version from `packageManager`), Node from `.nvmrc`, pnpm store caching via `actions/setup-node`.
- Branch-protection recommendation documented in README (require `CI / ci` check on `main`); applying it is a repo-settings action for the owner.
- Environment constraint recorded: no `gh` CLI or GitHub API credentials on this machine, so run status was verified by the repo owner in the browser rather than polled by CI tooling. If `gh auth login` happens later, exact run URLs can be appended here.
